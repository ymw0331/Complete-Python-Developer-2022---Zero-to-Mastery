# Dictionary

dictionary = {
    123: [1, 2, 3],
    True: 'hello',
    [100]: True
}

# dictionary key needs to be immutable (cannot be changed)
# key in dict has to be unique, only exist once

print(dictionary[True])  # index of 1
