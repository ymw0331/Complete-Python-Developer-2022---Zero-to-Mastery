# hash table in other programming language (key value pair)

# Dictionary (unordered pair, not right each other in memory, gather all across the memory)
dictionary = {
    'weapons': [1, 2, 3],
    'greetings': 'hello',
    'x': True
}
# list is sorted, it holds only single value
my_list = [{
    'a': [1, 2, 3],
    'b': 'hello',
    'x': True
}, {
    'a': [4, 5, 6],
    'b': 'hello',
    'x': True
}
]
print(my_list[0]['a'][2])
print(dictionary['a'][1])  # index of 1
