# Ternary operator
# shortcut for conditional logic/ conditional expression (version 2.4)

# condition_if_true if condition else condition_if_else

is_friend = True
can_message = "message allowed" if is_friend else "not allowed to message"

print(can_message)
